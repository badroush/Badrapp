{% extends 'base.html.twig' %}

{% block title %}دليل الهاتف{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h2 class="mb-0">📞 دليل الهاتف</h2>
            <a href="{{ path('app_contact_new') }}" class="btn btn-light">
                <i class="bi bi-plus-circle"></i> إضافة جهة اتصال
            </a>
        </div>
        <div class="card-body">
            <!-- Barre de recherche -->
            <form method="GET" action="{{ path('app_contact_index') }}" class="mb-4">
                <div class="input-group">
                    <input type="text" name="q" value="{{ app.request.query.get('q') }}" placeholder="البحث عن طريق الاسم أو الرقم" class="form-control">
                    <button type="submit" class="btn btn-outline-primary">
                        <i class="bi bi-search"></i> بحث
                    </button>
                </div>
            </form>

            <!-- Tableau des contacts -->
            {% if contacts is not empty %}
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-light">
                            <tr>
                                
                                <th scope="col">الاسم</th>
                                <th scope="col">الرقم</th>
                                <th scope="col">الفاكس</th>
                                <th scope="col">البريد الإلكتروني</th>
                                <th scope="col">العنوان</th>
                                <th scope="col">الإجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for contact in contacts %}
                                <tr>
                                    
                                    <td>{{ contact.nom }}</td>
                                    <td>{{ contact.numero }}</td>
                                    <td>{{ contact.fax ?: '-' }}</td>
                                    <td>{{ contact.email ?: '-' }}</td>
                                    <td>{{ contact.adresse ?: '-' }}</td>
                                    <td>
                                        <a href="{{ path('app_contact_show', {'id': contact.id}) }}" class="btn btn-sm btn-info me-1">
                                            <i class="bi bi-eye"></i> عرض
                                        </a>
                                        <a href="{{ path('app_contact_edit', {'id': contact.id}) }}" class="btn btn-sm btn-warning">
                                            <i class="bi bi-pencil"></i> تعديل
                                        </a>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="alert alert-info text-center border-info">
                    <i class="bi bi-info-circle"></i>
                    <p class="mb-0">لا توجد جهات اتصال.</p>
                </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}