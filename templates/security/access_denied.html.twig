{% extends 'base.html.twig' %}

{% block title %}وصول مرفوض{% endblock %}

{% block body %}
<style>
    .access-denied-wrapper {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .access-denied-card {
        max-width: 480px;
        border: none;
        border-radius: 1rem;
        animation: fadeInUp 0.6s ease-out;
    }

    .access-icon {
        font-size: 4rem;
        color: #dc3545;
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>

<div class="container access-denied-wrapper">
    <div class="card access-denied-card shadow-lg text-center">
        <div class="card-body p-4">
            <div class="mb-3">
                <span class="access-icon">🚫</span>
            </div>

            <h2 class="fw-bold text-danger mb-3">وصول مرفوض</h2>

            <p class="text-muted mb-4">
                ليس لديك الصلاحيات اللازمة للوصول إلى هذه الصفحة.
            </p>

            <a href="{{ path('app_home') }}" class="btn btn-primary px-4">
                ← العودة إلى الرئيسية
            </a>
        </div>
    </div>
</div>
{% endblock %}