{% extends 'base.html.twig' %}

{% block title %}إدارة موارد الميزانية{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h1 class="mb-0">💰 إدارة موارد الميزانية</h1>
            <a href="{{ path('app_ressource_budget_new') }}" class="btn btn-light">
                <i class="bi bi-plus-circle"></i> إضافة مصدر
            </a>
        </div>
        <div class="card-body p-0">
            {% if ressource_budgets is not empty %}
                <div class="table-responsive">
                    <table class="table table-hover align-middle" id="ressourceBudgetTable">
                        <thead class="table-light">
                            <tr>
                                <th>الرمز</th>
                                <th>الاسم</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for ressource in ressource_budgets %}
                                <tr>
                                    <td>{{ ressource.code }}</td>
                                    <td>{{ ressource.nom }}</td>
                                    <td>
                                        <a href="{{ path('app_ressource_budget_edit', {'id': ressource.id}) }}" class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <form method="post" action="{{ path('app_ressource_budget_delete', {'id': ressource.id}) }}" style="display: inline;" onsubmit="return confirm('هل أنت متأكد من حذف هذا المصدر؟');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.id) }}">
                                            <button class="btn btn-sm btn-outline-danger">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="text-center py-4 text-muted">لا توجد مصادر.</div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener("DOMContentLoaded", function() {
    new DataTable('#ressourceBudgetTable', {
        language: {
            "sProcessing": "جارٍ التحميل...",
            "sLengthMenu": "أظهر _MENU_ مدخلات",
            "sZeroRecords": "لم يُعثر على نتائج",
            "sInfo": "إظهار _START_ إلى _END_ من أصل _TOTAL_ مدخل",
            "sInfoEmpty": "إظهار 0 إلى 0 من أصل 0 مدخل",
            "sInfoFiltered": "(من أصل _MAX_ مدخل)",
            "sSearch": "بحث:",
            "oPaginate": {
                "sFirst": "الأول",
                "sPrevious": "السابق",
                "sNext": "التالي",
                "sLast": "الأخير"
            }
        },
        paging: true,
        pageLength: 15,
        searching: true,
        info: true,
        order: [[0, 'asc']]
    });
});
</script>
{% endblock %}