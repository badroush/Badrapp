{% extends 'base.html.twig' %}

{% block title %}Modifier Ressource Budget{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="card shadow-lg border-0">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h3 class="mb-0">Modifier Ressource Budget</h3>
        </div>
        <div class="card-body">
            {{ include('ressource_budget/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <a class="btn btn-secondary mt-3" href="{{ path('app_ressource_budget_index') }}">Retour à la liste</a>

    {{ include('ressource_budget/_delete_form.html.twig') }}
</div>
{% endblock %}