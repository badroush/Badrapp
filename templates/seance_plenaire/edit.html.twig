{% extends 'base.html.twig' %}

{% block title %}تعديل الجلسة العامة{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h2 class="mb-0">✏️ تعديل الجلسة العامة</h2>
        </div>
        <div class="card-body">
            {{ include('seance_plenaire/_form.html.twig', {'button_label': 'تحديث'}) }}
        </div>
    </div>
</div>
{% endblock %}