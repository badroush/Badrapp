{% extends 'base.html.twig' %}

{% block title %}{{ chapitre.nom }}{% endblock %}

{% block body %}
    <div class="container-fluid mt-5">
        <div class="row">
            <div class="col-12">
                <div class="card shadow-lg border-0">
                    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                        <h2 class="mb-0">🛒 {{ chapitre.nom }}</h2>
                        <p class="mb-0 text-light">اختر المواد وحدد الكمية</p>
                    </div>
                    <div class="card-body p-5">
                        <form method="post" action="{{ path('app_achats_collectifs_ajouter', {'id': chapitre.id}) }}">
                            <div class="row mb-4">
                                <div class="col-md-8">
                                    <label for="article" class="form-label">اختر المادة</label>
                                    <select name="article" id="article" class="form-select" required>
                                        <option value="">اختر مادة...</option>
                                        {% for article in articles %}
                                            <option value="{{ article.id }}">
                                               {{ article.prixVente|number_format(3, ',', ' ') }} -  {{ article.article.nom }} 
                                            </option>
                                        {% endfor %}
                                    </select>
                                </div>
                                <div class="col-md-3">
                                    <label for="quantite" class="form-label">الكمية</label>
                                    <input type="number" name="quantite" id="quantite" class="form-control" min="1" value="1" required>
                                </div>
                                <div class="col-md-1 d-flex align-items-end">
                                    <button type="submit" class="btn btn-success w-100">
                                        <i class="fas fa-plus">+</i>
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        .card {
            border-radius: 12px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }
        .btn {
            border-radius: 8px;
            font-weight: 500;
        }
    </style>
{% endblock %}