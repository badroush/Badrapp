<form method="post" action="{{ path('app_connexion_internet_delete', {'id': connexion_internet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ connexion_internet.id) }}">
    <button class="btn">Delete</button>
</form>
