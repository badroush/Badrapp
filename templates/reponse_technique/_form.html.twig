{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-secondary" href="{{ path('app_reponse_technique_index') }}">إلغاء</a>
        <button class="btn btn-success">{{ button_label|default('حفظ') }}</button>
    </div>
{{ form_end(form) }}