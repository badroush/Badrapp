<form method="post" action="{{ path('app_chapitre_budget_delete', {'id': chapitre_budget.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chapitre_budget.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>