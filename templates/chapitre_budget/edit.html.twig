{% extends 'base.html.twig' %}

{% block title %}Modifier Chapitre Budget{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="card shadow-lg border-0">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h3 class="mb-0">تحيين فصول الميزانية</h3>
        </div>
        <div class="card-body">
            {{ include('chapitre_budget/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <a class="btn btn-secondary mt-3" href="{{ path('app_chapitre_budget_index') }}">الرجوع</a>

    {{ include('chapitre_budget/_delete_form.html.twig') }}
</div>
{% endblock %}