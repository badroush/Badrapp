{{ form_start(form) }}
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.code, {'label': 'الرمز'}) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.nom, {'label': 'الاسم'}) }}
        </div>
    </div>

    <button class="btn btn-success mt-3">{{ button_label|default('حفظ') }}</button>
{{ form_end(form) }}