{% extends 'base.html.twig' %}

{% block title %}إضافة فصل ميزانية{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h2>➕ إضافة فصل ميزانية جديد</h2>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                <div class="row">
                    <div class="col-md-6">
                        {{ form_row(form.code, {'label': 'الرمز'}) }}
                        {{ form_row(form.nom, {'label': 'الاسم'}) }}
                    </div>
                </div>
                <button type="submit" class="btn btn-success mt-3">
                    <i class="bi bi-save"></i> حفظ الفصل
                </button>
                <a href="{{ path('app_chapitre_budget_index') }}" class="btn btn-secondary mt-3">
                    <i class="bi bi-arrow-return-left"></i> عودة
                </a>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}