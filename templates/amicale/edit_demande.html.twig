{% extends 'base.html.twig' %}

{% block title %}تعديل حالة الطلب{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>تعديل حالة الطلب</h1>

    {{ form_start(form) }}
        {{ form_row(form.statut, {'label': 'الحالة', 'attr': {'class': 'form-control', 'type': 'select'}}) }}
        <button class="btn btn-primary mt-3">تحديث الحالة</button>
    {{ form_end(form) }}

    <a href="{{ path('app_amicale_index') }}" class="btn btn-secondary mt-2">العودة إلى القائمة</a>
</div>
{% endblock %}