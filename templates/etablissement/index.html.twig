{% extends 'base.html.twig' %}

{% block title %}Liste des Établissements{% endblock %}

{% block body %}
<<<<<<< HEAD
=======
{% include "layout/header.html.twig" %}
>>>>>>> d6476d60ab6c137fd57e0e94d3b5e16948081daa
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>🏢 Liste des Établissements</h1>
        <a href="{{ path('etablissement_new') }}" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Ajouter un établissement
        </a>
    </div>

    <div class="card">
        <div class="card-body p-0">
            {% if etablissements is not empty %}
                <div class="table-responsive">
                    <table class="table table-hover align-middle" id="etablissementTable">
                        <thead class="table-light">
                            <tr>
                                <th>Nom</th>
                                <th>Adresse</th>
                                <th>Téléphone</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for e in etablissements %}
                                <tr>
                                    <td>{{ e.nom }}</td>
                                    <td>{{ e.adresse ?: '—' }}</td>
                                    <td>{{ e.telephone ?: '—' }}</td>
                                    <td>
                                        <a href="{{ path('etablissement_edit', {'id': e.id}) }}" class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <form method="post" action="{{ path('etablissement_delete', {'id': e.id}) }}" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet établissement ?');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ e.id) }}">
                                            <button class="btn btn-sm btn-outline-danger">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="text-center py-4 text-muted">Aucun établissement trouvé.</div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener("DOMContentLoaded", function() {
    new DataTable('#etablissementTable', {
        language: {
            "sProcessing": "جارٍ التحميل...",
            "sLengthMenu": "أظهر _MENU_ مدخلات",
            "sZeroRecords": "لم يُعثر على نتائج",
            "sInfo": "إظهار _START_ إلى _END_ من أصل _TOTAL_ مدخل",
            "sInfoEmpty": "إظهار 0 إلى 0 من أصل 0 مدخل",
            "sInfoFiltered": "(من أصل _MAX_ مدخل)",
            "sSearch": "بحث:",
            "oPaginate": {
                "sFirst": "الأول",
                "sPrevious": "السابق",
                "sNext": "التالي",
                "sLast": "الأخير"
            }
        },
        paging: true,
<<<<<<< HEAD
        pageLength: 10,
=======
        pageLength: 15,
>>>>>>> d6476d60ab6c137fd57e0e94d3b5e16948081daa
        searching: true,
        info: true,
        order: [[0, 'asc']]
    });
});
</script>
{% endblock %}
