{% extends 'base.html.twig' %}

{% block title %}تعديل عضو{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h2 class="mb-0">✏️ تعديل العضو</h2>
        </div>
        <div class="card-body">
            {{ include('adhesion/_form.html.twig', {'button_label': 'تحديث'}) }}

            <div class="mt-3">
                <a href="{{ path('adhesion_index') }}" class="btn btn-secondary">
                    <i class="bi bi-arrow-return-left"></i> عودة إلى القائمة
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}