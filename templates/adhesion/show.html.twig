{% extends 'base.html.twig' %}

{% block title %}تفاصيل العضو{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h2 class="mb-0">📋 تفاصيل العضو</h2>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th scope="row">الرقم</th>
                        <td>{{ adhesion.id }}</td>
                    </tr>
                    <tr>
                        <th scope="row">الاسم</th>
                        <td>{{ adhesion.nom }}</td>
                    </tr>
                    <tr>
                        <th scope="row">اللقب</th>
                        <td>{{ adhesion.prenom }}</td>
                    </tr>
                    <tr>
                        <th scope="row">تاريخ الولادة</th>
                        <td>{{ adhesion.dateNaissance ? adhesion.dateNaissance|date('Y-m-d') : '' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">العنوان</th>
                        <td>{{ adhesion.adresse }}</td>
                    </tr>
                    <tr>
                        <th scope="row">الحالة الاجتماعية</th>
                        <td>
                            {% if adhesion.etatCivil == 1 %}أعزب{% elseif adhesion.etatCivil == 2 %}متزوج{% elseif adhesion.etatCivil == 3 %}مطلق{% elseif adhesion.etatCivil == 4 %}أرمل{% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th scope="row">سنة الانضمام</th>
                        <td>{{ adhesion.anneeAdhesion }}</td>
                    </tr>
                    <tr>
                        <th scope="row">تاريخ الانضمام</th>
                        <td>{{ adhesion.dateAdhesion ? adhesion.dateAdhesion|date('Y-m-d') : '' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">الصورة</th>
                        <td>
                            {% if adhesion.photo %}
                                <img src="{{ asset('uploads/photos/' ~ adhesion.photo) }}" alt="Photo" width="100" height="100" class="rounded-circle">
                            {% else %}
                                <span class="text-muted">لا توجد صورة</span>
                            {% endif %}
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="mt-3">
                <a href="{{ path('adhesion_index') }}" class="btn btn-secondary">
                    <i class="bi bi-arrow-return-left"></i> عودة إلى القائمة
                </a>
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPER_ADMIN') %}
                
                <a href="{{ path('app_adhesion_edit', {'id': adhesion.id}) }}" class="btn btn-warning">
                    <i class="bi bi-pencil"></i> تعديل
                </a>

                {{ include('adhesion/_delete_form.html.twig') }}
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}